"""Обработка документов: разбор, классификация, извлечение полей, маппинг.

Слои:
    parsers/     — приведение файла к тексту и структуре
    extractors/  — детерминированное извлечение полей (regex + правила)
    mappers/     — сопоставление извлечённых полей с полями дела и заявления

Здесь нет ни FastAPI-роутов, ни UI-кода: модуль вызывается из сервисного
слоя и из скриптов в ``backend/scripts/``.
"""
