# Серверные исправления SRV-01–15 / Jira

Ветка: `fix/KAN-15-infrastructure-hardening`. Работа основана на `24dfec0`.
06.09.2026 Jira подтвердила у KAN-15 связанную ветку с источником GitHub.

## Изменения и приёмка

| Jira | Изменение | Проверка |
| --- | --- | --- |
| KAN-4, KAN-5 | Общая проверка владельца/назначенной роли для API дел, рисков, черновиков и MVP; application_id в аудите решения | Матрица чужого клиента/юриста, отказ до запуска |
| KAN-6 | Сверка ожидаемого статуса под блокировкой; служебные результаты нельзя выставить через общий transition | Запрет подмены submitted и устаревшего перехода |
| KAN-8 | Принадлежность заявителя; назначение только администратором с проверкой активной роли; запрет общего изменения submitted/closed | Проверки доступа и назначения |
| KAN-9, KAN-35 | Версия сессий, смена/сброс пароля и logout-all отзывают токены; bcrypt с SHA-256 обрабатывает весь пароль | Старые токены, длинные и Unicode-пароли |
| KAN-10 | Общие IP/пользовательские квоты, группы маршрутов, очистка TTL, ограничение количества ключей | Просроченные ключи и насыщение лимитера |
| KAN-11 | Отдельный процесс разбора, таймаут с завершением OCR-процессов на Linux, лимиты страниц/пикселей/распаковки DOCX | Настоящий дочерний процесс и повреждённый/сжатый DOCX |
| KAN-12 | Квоты заявок/документов/очереди; общий дневной бюджет запросов LLM в БД; лимиты контейнеров и логов | Атомарное резервирование бюджета; требуется нагрузочная приёмка VPS |
| KAN-13 | Атомарная публикация файла; физическое удаление только offline GC после фиксации и периода хранения | Rollback сохраняет запись и оригинал |
| KAN-14 | Бэкап с остановкой записывающих сервисов, закрытыми правами, manifest/SHA-256 и restic; проверка восстановления в отдельный контейнер | CI; внешнее хранилище и расписание на VPS ещё нужно настроить |
| KAN-15 | Точное совпадение Alembic heads, heartbeat worker, возраст очереди, свободный диск и публичный /ready | Регрессии и production smoke в CI |
| KAN-36 | Lock-файлы с хешами, digest Python-образа, аудит зависимостей CI | pip-audit; уязвимая транзитивная ecdsa удалена заменой JWT-библиотеки |
| KAN-17 | PostgreSQL-миграции, nginx/API, отдельный worker, проверка остановки worker и восстановления бэкапа | Отдельный job production-smoke |

06.09.2026 на registr-ai.ru установлен `c1bb57b` с сохранением ранее
незакоммиченных изменений сервера. `/opt/registr` указывает на
`/opt/registr-releases/c1bb57b`; прежний каталог сохранён в
`/opt/registr-before-c1bb57b`, прежние образы — `registr-rollback-*:before-3edb134`.
API, worker, PostgreSQL и nginx healthy; публичный `/ready` проверяет базу,
хранилище, heartbeat и очередь. Ограничения памяти/CPU применены,
OOM и перезапусков при контрольной проверке нет. Неавторизованное чтение
результатов возвращает 401. Активных аккаунтов с публичным демо-паролем не найдено.

Перед обновлением проверено восстановление базы и трёх ссылок на документы:
контрольные суммы совпали, проверка заняла 3 секунды на текущем небольшом наборе.
Комплект хранится в `/root/registr-rollout/backups`; отдельная копия сохранена
на компьютере владельца как `.runtime/pre-3edb134.tar.gz.dpapi`, зашифрована
Windows DPAPI для его учётной записи; расшифровка и хеш проверены.
Эта разовая копия не заменяет настройку ежедневного внешнего restic-бэкапа.
Нагрузочная приёмка и автоматическое внешнее резервирование остаются открытыми.

При первой попытке nginx не смог прочитать конфигурацию, скопированную из
репозитория с umask 077. `COPY --chmod=644` исправил права без запуска nginx от root.
При возврате старого приложения после добавочной миграции старый контейнер
`migrate` не запускают: он не знает новой ревизии. Для аварийного возврата
сохранённых образов использован `up -d --no-deps --no-build`; база не откатывалась.

## Развёртывание

1. Перед обновлением сохранить прежний образ, ревизию и согласованный бэкап.
2. Задать отдельный случайный `SECRET_KEY` (например, `openssl rand -hex 32`).
   `ENVIRONMENT=production` задаётся Compose принудительно. Шаблонный ключ и DEBUG
   останавливают запуск; ошибки конфигурации не печатают значения входа.
3. Подготовить `/etc/registr-backup.env` с правами `600`: `RESTIC_REPOSITORY`,
   `RESTIC_PASSWORD_FILE`, необходимые учётные данные внешнего хранилища.
   Хранилище выбирает владелец проекта; секреты не сохраняются в репозитории.
4. Инициализировать restic-репозиторий, запустить `bash deploy/backup.sh`.
   Ожидать комплект `backups/backup-*` с `SHA256SUMS` и `OFFSITE_OK`.
5. Запустить `bash deploy/restore-check.sh /absolute/path/to/backup-*`.
   Скрипт поднимает отдельный временный PostgreSQL, восстанавливает dump и
   сверяет SHA-256 каждого документа. Рабочая БД не изменяется.
6. После приёмки изменений выполнить `bash deploy/deploy.sh`. Скрипт проверяет
   `/ready` через API и публичный прокси; `/healthz` — только liveness nginx.
7. Установить `deploy/registr-backup.service` и `.timer` в `/etc/systemd/system`,
   затем `systemctl daemon-reload` и `systemctl enable --now registr-backup.timer`.
   Проверять результат через `systemctl status registr-backup.service`.

Бэкап кратковременно останавливает API и worker, но автоматически запускает
ранее работавшие сервисы и при ошибке. Для круглосуточного сервиса в дальнейшем
потребуется backup без окна обслуживания. RPO при ежедневном расписании — до
24 часов; фактический RTO измеряет restore-check и подтверждает оператор.
Внешнюю копию тоже следует периодически восстанавливать через restic restore
в отдельный каталог и проверять этим же скриптом.

`BACKUP_LOCAL_ONLY=true` предназначен для изолированного CI/локальной проверки:
он явно не подтверждает наличие внешней копии. Локальные комплекты автоматически
не удаляются, пока не подтверждена политика хранения. Следить за заполнением диска.

## Лимиты и обслуживание

Начальные лимиты: 100 заявок, 200 документов / 250 МБ на пользователя;
2 активных задания пользователя, 20 суммарно. LLM резервирует дневной бюджет
в общей БД до каждого запроса, включая повторы: размер JSON в UTF-8 плюс верхний
лимит ответа. Это консервативные единицы расхода, а не сумма в рублях; лимиты
по умолчанию — 2 млн на пользователя и 10 млн на стенд, сброс по UTC.
Одновременно допускается один запрос LLM на процесс (API и worker раздельно).

На 3 ГБ VPS заданы максимумы: PostgreSQL 512 МБ, API 1152 МБ (включая парсер),
worker 640 МБ, nginx 96 МБ, Caddy 128 МБ. Это потолки, а не резервы; отдельная
нагрузочная проверка нужна перед расширением использования. При нехватке 512 МБ
диска загрузки отклоняются, `/ready` становится неготовым. Логи — 3 × 10 МБ
на контейнер. Внешний мониторинг должен проверять именно `/ready`.

Физический GC: `bash deploy/maintenance.sh`, по умолчанию хранит сироты 7 дней.
Запускается только с остановленными API/worker под общей maintenance-блокировкой;
повторно сверяет ссылки в БД. Прямой запуск GC при работающих писателях запрещён.

Старые bcrypt-пароли до 72 байт обновляются при успешном входе. Для ранее
обрезавшихся паролей длиннее 72 байт нужен административный сброс: угадывать
отброшенный суффикс нельзя. После смены пароля интерфейс предлагает повторный вход.
Общий `/transition` теперь требует `expected_status`; результаты обработки
выставляют профильные операции, а не произвольный запрос смены статуса.

## Обновление зависимостей

```sh
uv pip compile backend/requirements.txt --python-version 3.12 --python-platform linux --generate-hashes -o backend/requirements.lock
uv pip compile backend/requirements-dev.txt -c backend/requirements.lock --python-version 3.12 --python-platform linux --generate-hashes -o backend/requirements-dev.lock
```

CI сохраняет ID/метаданные собранных образов и перечни Python, Debian и Alpine
пакетов в артефакте `production-image-inventory`.
Обновление lock-файлов и digest образа проходит тот же CI. Аудит Python-пакетов не
заменяет инвентаризацию и сканирование реально развёрнутого образа на сервере.
